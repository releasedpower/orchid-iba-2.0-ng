import { DateToFrenchPipe } from './date-to-french.pipe';

describe('DateToFrenchPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToFrenchPipe();
    expect(pipe).toBeTruthy();
  });
});
